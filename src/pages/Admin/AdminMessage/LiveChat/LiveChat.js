import React from 'react';
import useTitle from '../../../../hooks/useTitle';

const LiveChat = () => {
    useTitle('Live Chat')
    return (
        <div>
            <h2 className="text-2xl">live chat page</h2>
        </div>
    );
};

export default LiveChat;