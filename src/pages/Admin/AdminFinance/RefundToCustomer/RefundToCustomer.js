import React from 'react';
import useTitle from '../../../../hooks/useTitle';

const RefundToCustomer = () => {
    useTitle('Refund Customer');
    return (
        <div>
            <h2 className="text-2xl">Refund to Customer page</h2>
        </div>
    );
};

export default RefundToCustomer;