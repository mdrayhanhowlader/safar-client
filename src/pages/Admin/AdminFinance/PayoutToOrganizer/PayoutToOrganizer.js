import React from 'react';
import useTitle from '../../../../hooks/useTitle';

const PayoutToOrganizer = () => {
    useTitle('Payout Organizer');
    return (
        <div>
            <h2 className="text-2xl">payout to organizer</h2>
        </div>
    );
};

export default PayoutToOrganizer;